# Portakal 1.0.0 Release Evidence Record

- **Release Version**: `1.0.0`
- **Release Date**: 2026-08-25
- **Status**: Ready for Final Publication
- **License**: MIT License

---

## 1. Package Metadata & Final Versions

| Package | Final Version | Pub Topics | Archive Size | Dependencies |
|---|---|---|---|---|
| `portakal_core` | `1.0.0` | `printer`, `thermal-printer`, `esc-pos`, `zpl`, `tspl` | 161 KB (66 files) | *None* (Pure Dart) |
| `portakal_flutter` | `1.0.0` | `printer`, `thermal-printer`, `flutter`, `label`, `esc-pos` | 7 KB (9 files) | `flutter`, `portakal_core: ^1.0.0` |

---

## 2. Release Candidate Validation Summary

Prior to final 1.0.0 promotion, `1.0.0-rc.1` was published and verified in external, isolated environments:
- **`portakal_core: 1.0.0-rc.1` Hosted Resolution**: Successfully installed from `pub.dev`. Executed pure Dart console suite verifying `EscPosPrinter`, `TscPrinter`, `LabelBuilder`, `zpl.compile`, `ReceiptColumn`, and `imageToMonochrome` with all tests passing.
- **`portakal_flutter: 1.0.0-rc.1` Hosted Resolution**: Successfully installed from `pub.dev`. Executed Flutter widget test confirming `LabelPreview` rendering alongside Flutter `Column` and `ReceiptColumn` with zero symbol collisions.
- **Dependency Graph**: Confirmed pure hosted resolution without path dependencies, workspace overrides, or git references.

---

## 3. Minimum Supported Platforms & Toolchains

| Component | Minimum Constraint | Tested SDK Baseline | Status |
|---|---|---|---|
| `portakal_core` | `sdk: "^3.6.0"` | Dart 3.6.0+ | **PASS** |
| `portakal_flutter` | `sdk: "^3.6.0"`, `flutter: ">=3.27.0"` | Flutter 3.27.0+ (bundles Dart 3.6.0) | **PASS** |

---

## 4. Verification Baseline

| Verification Gate | Command | Result |
|---|---|---|
| Code Formatting | `dart format --output=none --set-exit-if-changed .` | **PASS (0 unformatted files)** |
| Core Static Analysis | `dart analyze --fatal-infos packages/portakal_core` | **PASS (0 issues)** |
| Flutter Static Analysis | `flutter analyze --fatal-infos packages/portakal_flutter` | **PASS (0 issues)** |
| Example Static Analysis | `flutter analyze --fatal-infos example` | **PASS (0 issues)** |
| Core Tests | `dart test packages/portakal_core` | **PASS (817 tests passing)** |
| Flutter Tests | `flutter test packages/portakal_flutter` | **PASS (6 tests passing)** |
| Example Tests | `flutter test example` | **PASS (16 tests passing)** |
| **Total Test Suite** | Workspace Test Suite | **PASS (839 tests passing, 0 failures)** |
| Core Dartdoc | `dart doc --dry-run packages/portakal_core` | **PASS (0 errors, 0 warnings)** |
| Flutter Dartdoc | `dart doc --dry-run packages/portakal_flutter` | **PASS (0 errors, 0 warnings)** |
| API Surface Freeze | `git diff --exit-code tool/api_surface.txt` | **PASS (0 drift against 1.0 freeze)** |
| Markdown Link Integrity | Markdown Link Audit Script | **PASS (0 broken links across 34 docs)** |
| Core Publish Dry-Run | `dart pub publish --dry-run` | **PASS (0 errors, 0 warnings)** |
| Flutter Publish Dry-Run | `flutter pub publish --dry-run` | **PASS (0 errors, 0 warnings)** |

---

## 5. Final Publication Sequence

Publication must follow this strict sequence upon manual operator execution:

### Step 1: Publish `portakal_core 1.0.0`
```bash
cd packages/portakal_core
dart pub publish
```
*Verification*: Confirm presence on [pub.dev/packages/portakal_core](https://pub.dev/packages/portakal_core).

### Step 2: Core External Smoke Test
```bash
rm -rf /tmp/portakal_core_1_smoke
mkdir -p /tmp/portakal_core_1_smoke && cd /tmp/portakal_core_1_smoke
dart create -t console-simple .
dart pub add portakal_core:1.0.0
dart analyze
dart run
```

### Step 3: Publish `portakal_flutter 1.0.0`
```bash
cd packages/portakal_flutter
flutter pub publish
```
*Verification*: Confirm presence on [pub.dev/packages/portakal_flutter](https://pub.dev/packages/portakal_flutter).

### Step 4: Flutter External Smoke Test
```bash
rm -rf /tmp/portakal_flutter_1_smoke
mkdir -p /tmp/portakal_flutter_1_smoke && cd /tmp/portakal_flutter_1_smoke
flutter create -t app .
flutter pub add portakal_flutter:1.0.0
flutter analyze
flutter test
```

### Step 5: Git Tagging
```bash
git tag -a v1.0.0 -m "Release Portakal 1.0.0"
git push origin v1.0.0
```

---

## 6. Hardware Validation Summary

- **ESC/POS**: Physical validation passed on `Printer001-328F` (text formatting, line spacing, paper feed, alignment).
- **TSC/TSPL2**: Physical validation passed on `Printer001-328F` (label sizing, coordinate positioning, bitmap rendering, 1D/2D barcodes).
- **ZPL II**: Identified as unsupported hardware firmware (`N/S-DEVICE`) on `Printer001-328F`; compiler verified at Level 1 Byte Golden standard.
- **Golden Byte Suites**: 9-protocol deterministic byte golden assertions verified.

---

## 7. 1.x Stability Contract

All public APIs documented in `tool/api_surface.txt` are frozen for the 1.x lifecycle. Breaking changes, removals of deprecated aliases (`Column`, `compileBytes`, `raw(Object)`, `encodeTextForPrinter`, and String serializers), and architectural overhauls remain deferred to 2.0.
